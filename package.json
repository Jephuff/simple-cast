{
  "name": "simple-cast",
  "version": "0.4.2",
  "description": "",
  "main": "dist",
  "scripts": {
    "prepare": "mkdir -p dist && rm -r dist && mkdir -p dist && npm run build",
    "watch": "npm run build -- -w",
    "build": "mkdir -p dist && rm -r dist && mkdir -p dist && tsc --outDir dist -d"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Jephuff/simple-cast.git"
  },
  "keywords": [
    "chromecast",
    "chrome",
    "cast",
    "video",
    "media",
    "simplecast"
  ],
  "author": "Jeffrey Burt",
  "license": "ISC",
  "dependencies": {
    "eventemitter3": "4.x"
  },
  "devDependencies": {
    "@types/chrome": "0.0.x",
    "@types/chromecast-caf-sender": "1.x",
    "@typescript-eslint/eslint-plugin": "1.x",
    "eslint": "6.x",
    "eslint-config-prettier": "6.x",
    "eslint-plugin-prettier": "3.x",
    "prettier": "1.x",
    "typescript": "3.x"
  }
}
